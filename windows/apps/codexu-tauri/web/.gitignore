node_modules/
dist/
.cache/
# Workspace-local npm cache/temp, used when the host blocks the default
# locations. Never committed.
.npm-cache/
.npm-tmp/
*.log
.env
.env.local
# package-lock.json is committed for reproducible npm ci builds.
