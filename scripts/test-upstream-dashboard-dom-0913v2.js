'use strict';
// SUPERSEDED by test-upstream-dashboard-dom-0918v1.js (2026-09-18 rewrite).
// This 0913-era spec asserted the pre-0915 dashboard contract (calendar status
// classes, "已确认" inside the selection line, reset annotations rendered by
// the web view) and fails against the current design by rule, not by
// regression. Kept only as a pointer; do not wire into CI.
console.log('SKIP test-upstream-dashboard-dom-0913v2.js: superseded by test-upstream-dashboard-dom-0918v1.js');
