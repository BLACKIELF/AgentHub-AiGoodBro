# AiGoodBro 9.6.8 (57) · 0922v3

相较 9.6.7 (56) / 0922v2，新增「关于」小助理联系方式；上一版的账号、公开预告、调度与 CLI 修复继续保留。

## 变化

- 微信号 `AiGoodBro`，支持选择文本、一键复制和复制结果反馈。
- 二维码完整复用用户指定的 2026-09-22 教程原图；缩略图按原比例展示，点击放大，固定关闭按钮与 Esc 退出。二维码随应用打包，离线可见；资源不可读时仍可复制微信号。
- 菜单「关于 AiGoodBro」与设置侧栏「关于」进入同一页面；已有设置窗口重复打开时也能正确切回，其他设置保存逻辑不变。
- 普通窗口横向排列，空间不足时自动上下排列。未添加未经用户提供的服务承诺。

## 来源与审查

原图来自当天已推送飞书的 Codex 教程 0922v4 本地交付包。原图、教程 DOCX 的 `word/media/image1.jpg`、应用资源逐字节一致；SHA-256 为 `863e032523009da9acffd04170d17f8913f5b35d891c8601ff64110f992c8e68`。本机二维码识别得到微信地址；未自动添加好友或发送消息。

用户指定 Pro 5x / 5.6 Sol High 完成独立只读审查，未发现 P1/P2，要求实际验证反复打开、窄窗口与复制反馈。Jev 文字预审对「是否还需修复」仅 0.46 置信度，不作为通过或否决证据；以编译、原生 UI 和解码验证为准。Jev 持续参与实际控件决策。

本轮额外使用的 `write-swift`、`lark-shared`、`lark-im` 经检视及 Jev 建议保留（0.92），没有已复现的 Skill 缺陷。飞书认证状态只读核验后即找到了明确的本地交付记录，无需扩大检索、重新授权或发送消息。

## 验证与交付

- 正式优化构建、严格签名、设置展示自测、macOS 兼容检查和随包资源检查通过。
- 已备份 9.6.7 并覆盖安装 `/Applications/AiGoodBro.app`，启动后读取版本 9.6.8 (57)；可执行摘要 `8a9c5ef098ca1361d918306d049f0a94d865a94bb4847135fec7eb1cde0a737a`。
- 原生关于页已实际显示微信号和二维码；复制动作得到成功反馈，固定关闭与 Esc 均能关闭放大图，从其他设置页面再次选择菜单「关于」可以正确返回。
- 当前窄设置窗口截图中内容无重叠；二维码缩略图和放大图的实际截图均由本机识别器解码，与原图微信地址摘要一致。未启用 VoiceOver 或验证微信端好友添加结果。
- 首页预告、Pro 5x 与已退出调度状态在重启后仍可读；9.6.7 的回归结果见[上一版记录](release-notes-v9.6.7.md)。
- Jev 提供实际目标选择；菜单偶遇失效索引时由 Codex 重新读取定位。低置信度文字预审保留为不确定意见，没有通过反复投票替代代码与实机验证。

详细本机证据位于忽略目录 `.local-artifacts/pro5x-collaboration-0922v1/`。本轮未提交、推送、打标签或发布远端版本。
