# AiGoodBro 原生界面实现 · 0923v8

相较 0923v7：把已确认的首页骨架落到实际 SwiftUI 视图，默认深色「液态键帽」，保留原有 7 套配色、系统字体及图标；新增共享毛玻璃表面和实色回退，收紧首页账号卡片／列表与公告，补齐紧凑页脚入口。先交付独立体验版，后按用户明确的「覆盖安装」指令更新正式应用；代码仍在本地，未声明 GitHub 已更新。

## 正式覆盖安装 · 2026-09-23

- 已更新 `/Applications/AiGoodBro.app`，使用正式生产入口，保留原 bundle 标识与 9.6.9（58）版本号；本次 UI 增量标记为 0923v8。
- 通过原生退出快捷键关闭旧版，空闲门禁通过后复用 Makefile 的暂存、签名验证与替换流程。原应用额外备份在 `.local-artifacts/glass-0923v8-review/backups/AiGoodBro-before-0923v8.app`。
- 安装后主程序 SHA-256 与已测试构建一致，严格签名验证通过。实际启动已确认首页、11 个真实账号及额度显示，数量与安装前一致；按照用户指定在应用内选择液态键帽，深色保持开启，设置读回一致。
- 此次正式版的界面操作工具可用；已验证启动、主题菜单选择与主题持久化。它补充了此前预览版的工具超时记录，不代表所有账号动作已逐项验收。
- 回执：`.local-artifacts/glass-0923v8-review/overwrite-installation.json`。安装器未写入账号数据；未执行登录、Desktop 切号、暖号或重置兑换。

## 已实现

- 新设置使用深色液态键帽；已有有效的主题、外观及布局选择保留。损坏主题 ID 仍回退到旧安全主题并提示，不偷偷替换已保存的有效选择。
- 首页卡片与列表复用同一账号事实：稳定编号、到期信息、美元／点数、5 小时／7 天额度、重置时间与倒计时。零与未知分开；过期的历史会员日期标为待核实，点数最多保留两位小数。
- 导航、背景、卡片、列表及「推荐与公告」使用统一表面；装饰层不拦截点击，系统减少透明度或增强对比度时回退实色。
- 首页的完整账号操作仍通过管理入口进入；保留原管理页、模型与调度、用量图表、截图、引导、About／全部致谢与联系方式。页脚可重新展开用量统计，主题与外观可直接切换。
- Windows Web 同步默认值、7 主题、玻璃表面、紧凑公告、卡片／列表、编号与偏好持久化；原生 Rust 默认值已改，待 Windows 构建验证。

## 独立安装

- 应用：`/Applications/AiGoodBro Glass Preview.app`。
- 标识：`com.blackielf.aigoodbro.glasspreview0923v8`，构建标记 `0923v8`。
- 体验版使用生产 SwiftUI 视图与 8 个合成账号，启动入口固定为 `--preview-home-interaction`。页面时间固定为北京时间 2026-09-23 03:00，方便与 HTML 对照；不把它当成实时账号状态或真实账号操作验收。
- 安装前检查独立目标不存在；复制后严格校验签名、启动器与主程序 SHA-256。原版及两份原有预览版的标识和主入口校验值保持不变。安装后直接执行该包的主题自检通过。
- 安装回执：`.local-artifacts/glass-0923v8-review/installation.json`；安装前状态：同目录 `installed-before.json`。

## 验证与修复

Mac 最终构建通过。主题、设置／状态栏、主窗口布局、截图、用量界面五组自检全部通过；最终截图来自同一构建，覆盖 1440／820 点宽度、卡片／列表及浅色实色回退。

可调度账号 D 以 5.6 Sol／High 独立只读审查，任务完成后已释放。已修复装饰层命中测试、预览时钟隔离、旧主题损坏回退、列表低额度提示等问题。Jev 提供文本方案判断，建议以生产视图同尺寸对照；本轮没有把 Jev 文本判断当成图片检查。

截图检查发现，未挂载视图直接读取 Environment 会丢失预览时间与预告，导致全页和视口图不一致。现在由仅在 `store.isPreview` 时生效的显式参数传入，在生产入口强制忽略示例时间；最终两种截图使用同一事实和时间。浅色警示文字也改为按外观选择有对比度的颜色。

Windows 协作实现的 Web 构建、31 项单元／契约检查、31 项浏览器视觉／交互检查及后续 12 项 Dashboard 补测通过。它们验证 Web 层，不能代替 Windows Tauri、系统设置或安装验收。

## 仍未验证

- 独立预览版的界面工具曾三次超时；正式覆盖安装时已可读取界面并选择主题。仍未完成真实账号管理、调度开关或全部弹窗操作的逐项验收。
- 实色回退截图使用预览注入，未自动修改或实测 macOS 的系统辅助功能开关。
- 当前主机没有 Windows 原生环境及 Rust/Cargo。Windows 原生构建、终端进程、系统对比度、MSI／NSIS 打包与安装升级须按现有 Windows 交接继续执行。
- 本轮源码仍在本地工作树，未新增 Git 提交或推送；旧 PR 不自动包含本次增量。

## 对照与续接

- HTML：`docs/ui-preview-0923v7/index.html`，页面版本 0923v8；此前发送飞书的单文件 HTML 保留原样。
- 原生最终图：`.local-artifacts/glass-0923v8-final-renders/`，卡片和列表文件名分别为 `home-cards-dark-liquid-keycap-1440-full.png`、`home-rows-dark-liquid-keycap-1440-full.png`。
- 构建：`.local-artifacts/glass-0923v8-final-build/AiGoodBro.app`；自检回执：`.local-artifacts/glass-0923v8-review/test-summary.json`。
- [Windows 接手入口](windows-port/WINDOWS_AI_HANDOFF_0922v4.md)。后续继续尚未完成的原生交互验收和代码发布；正式覆盖安装已完成，不重复安装或从旧效果图重新实现。
