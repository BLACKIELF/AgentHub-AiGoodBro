# 账号识别、日历移除与界面方案评审 · 0923v6

相较 0923v5：用户指定 [Chan Karunamuni 的 Liquid Glass 介绍](https://x.com/chan_k/status/1932185261162766459) 为主要设计语言参考。已读取公开原帖文字、帖内指向的 Apple 官方讲解，并查看官方视频抽帧。后续材质以这项新参考为准，Stats / Zed / Linear 仅保留数据密度与对齐方法的借鉴；0923v5 是平面基线，不能当作已经采用新玻璃语言的成品。

## 0923v6 最新方向与样稿

Apple [Meet Liquid Glass](https://developer.apple.com/videos/play/wwdc2025/219/) 把玻璃定义为悬浮在内容上的导航与控件层，数据表仍属于内容层；提醒避免玻璃叠玻璃。结合这项说明，本项目的设计映射如下，这是设计方案而非已上线行为：

| 区域 | 本版方向 | 保留要求 |
| --- | --- | --- |
| 顶部导航、卡片/列表切换、菜单 | 采用玻璃层的细亮边、轻阴影、适当透明度与连续展开；生产实现优先系统 Regular 材质。 | 原系统字体、原 PNG 与现有图标；紧凑尺寸；导航不挤压信息区。 |
| 账号卡片、表格、用量工具 | 不透明深灰内容层，轻分隔；数值和时间明确分组。 | 原额度条和全部必需字段；未知不是 0；两种首页布局共用数据和编号。 |
| 管理页、公告与辅助功能 | 沿用已确认的信息架构，玻璃不侵入每个单元格和卡片。 | 管理页全部能力、模型调度默认展开、推荐与公告单行折叠、截图、引导、关于、全部致谢；仅删除日历。 |
| 动态与跨平台 | 展开短且可打断；减少动态、减少透明度时可退回稳定不透明控件。 | Windows 保持同样信息层级与功能，不称其为 Apple 原生光学效果；兼容性需要各平台验证。 |

本版两张图是同一份示例数据的首页代表样稿：`home-cards-0923v6.png`、`home-list-0923v6.png`。系统字体和真实资源由 SwiftUI 渲染；顶部与切换器的玻璃外观是静态材质近似，**不是原生折射、透视或形变已经验证的证据**。已实际查看两图，核对 01–08 编号、Pro 5x、会员到期、5 小时/7 天、重置日期及倒计时、美元/点数、紧凑公告、截图及页脚入口。正式功能与编号持久化未因出图而被宣称完成。

本机 macOS 27.0 / SDK 26.5 可以编译官方 `glassEffect` 接口，但离线 `ImageRenderer` 和 `NSHostingView` 缓存导图都未完整捕获系统材质；两次有界检查后停止继续尝试。当前图用于排版和材质范围评阅，真实折射与动态效果需要后续实际界面验证。参考 API：[Applying Liquid Glass to custom views](https://developer.apple.com/documentation/swiftui/applying-liquid-glass-to-custom-views)。本轮没有改写正式布局、覆盖安装或发布。

D 账号再次完成独立文字反方审查，运行参数为 5.6 Sol / High / 标准速度，正文与成功回执哈希一致，租约已验收收尾。采纳逐项字段验收、避免玻璃叠层、可访问性验证三项建议；这不是看图评分。Jev 在约 1.03 秒的单次有界文字判断中选择控件层方案，置信度 1.0；仍仅作为规则映射建议。图面检查由主代理完成。

当前交付入口为 `consolidated-0923v3/PREVIEW_INDEX.md`，只指向本版卡片和列表。完整管理页尚未按此新材质重绘，不混入旧稿。

## 历史设计研究与候选修复记录

以下 0923v1–v5 是有日期的历史记录；与上述最新参考、显示编号定义或交付范围冲突时，以 0923v6 为准。旧图不代表用户已经接受，新图不等于功能验收通过。


相较 0923v4：用户再次指出设计语言跑偏；0923v4 图稿不再作为后续视觉依据。实际检索 GitHub、读取官方设计说明并查看官方界面，收敛到原版系统字体、真实图标资源、紧凑数据分组及明确对齐。新增 SwiftUI 离线首页卡片/列表样稿，先验证代表页面的视觉尺度，管理页完整功能要求继续有效；尚未改写正式布局、安装或发布。

## 0923v5 实际参考与设计修正

检索与检查日期：2026-09-23。AnySearch 完成发现，GitHub API 核对热度；官方图例和 Firecrawl 页面截图用于视觉检查，未将营销官网的大标题与背景装饰用于应用。

| 一手参考 | 实际观察与采用范围 | 对 AiGoodBro 的调整 |
| --- | --- | --- |
| [Stats](https://github.com/exelban/stats)，查询时 42,053 stars；[官方界面](https://mac-stats.com/) | 紧凑的监控模块将数值、图表与明细分组；小图表辅助读数。 | 5 小时与 7 天并列；百分比为第一层，绝对重置时间与倒计时成组；保留原额度条，未知窗口不画零进度。 |
| [Zed](https://github.com/zed-industries/zed)，查询时 90,721 stars；[官方展示](https://zed.dev/) | 官网中的产品展示采用紧凑工具栏、窄分隔、清楚的侧栏与工作区边界。 | 次要按钮回到小工具栏尺度，操作组与数据组分开；不照搬代码字体或网页装饰。 |
| [Linear 官方改版说明](https://linear.app/now/how-we-redesigned-the-linear-ui) | 文章及前后图强调降低噪声、增强对齐和密度；图标、标签、按钮共用对齐线，页面靠灰阶和边界建立层级。 | 减少嵌套容器；账号、数值、辅助时间分层；列表固定列，整行统一对齐；不复制 Linear 品牌或整套样式。 |

这三项分别是数据密度、控件尺度与对齐方法的参考；视觉基底仍是 AiGoodBro 原版。Stars 仅表示社区关注，不作为设计质量评分。以上布局取舍是本任务设计判断。

当前代表页面的具体尺度：1280pt 宽；系统默认字体，账号名 13pt/semibold，卡片额度 22pt/semibold，列表额度 15pt/semibold，说明和日期 11–12pt；账号头像 23pt、导航 16pt；按钮视觉高度 27pt；账号卡片约 301×236pt、间隔 12pt、圆角 8pt。上述为桌面样稿起点，不把固定高度硬套到长名称、大字或窄窗。

- 原版 `AiGoodBro-icon.png`、`codex-color.png`、`claudecode-template.png` 被真实引用；无 PNG 的服务沿用仓库当前图形定义和系统符号，不用生成图近似替换。Claude 模板图标需按模板着色，避免在暗背景变黑。
- 同一示例 fixture 生成卡片和列表；编号 01–08、Pro 5x、未知值、会员到期、重置时间和倒计时一致。图稿只演示显示，不证明编号持久化或倒计时已运行。
- 推荐与公告收为 35pt 单行，保留待确认预告与入口。独立折叠、完整管理、截图、引导、关于、全部致谢与原用量工具继续按既有需求实现；没有新增删除。
- D 账号实际完成新一轮只读文字反方审查（5.6 Sol / High / 标准速度），成功回执与正文哈希一致，租约已验收。采纳「字段齐全不能证明审美合格」和「强弱层级不能平均分配」；不把其文字判断当作看图结果。
- Jev 参与验证方法的文字判断，约 1.53 秒选择原生代表页，置信度 1.0；仅作为方法建议，没有用它给视觉质量打分。
- 原生样稿在项目产物目录 `consolidated-0923v3/home-cards-0923v5.png` 和 `home-list-0923v5.png`；两种布局都只是首页视觉校准。管理页 0923v4 属于旧草稿，不能与本版拼成已完成的新三视图。

相较 0923v3：采用用户最新标注，所有账号均有可自定义的显示编号，编号表达用户排列顺序并持久保存；不再把内部调度字母当作该显示编号。恢复原版字体、缩小头像/CLI 图标及操作按钮，优先复用已有 PNG。0923v3 未完成出图批次已停止，避免继续交付旧编号方案。

相较 0923v2：按用户“理清需求、重新作图、调用可调度账号”的指令启动统一重绘。补齐 Pro 5x 正确套餐与缺失窗口案例，三视图共用示例数据、固定编号和同一时间基准；新增 D 账号的独立需求审查和 Jev 页面分工文字判断。图稿仍为候选，不能当作已批准或已安装。

相较 0923v1：统一后续出图使用的最新需求清单，明确旧图只作参考、局部认可不等于整稿通过，补齐跨页面一致性与交付边界。沿用本文件路径，避免产生多个互相冲突的评审入口。

0923v1 相较 0922v4：修复账号编号和详情归属不一致，移除已明确要求删除的重置日历，记录本轮界面预览与功能保留边界。本记录属于候选源码；尚未覆盖安装，也未发布新版本。

## 已确认的范围

- 仅删除用户明确要求移除的重置日历、翻月与按日筛选。保留公开预告、真实倒计时、近期记录、完整原文、逐条翻译和来源入口。
- 后续任何功能删除，先说明项目、理由和影响，由用户逐项确认。本轮不因猜测使用频率而删除功能。
- 截图方案、添加账号引导、全部致谢、关于 AiGoodBro、微信号复制与二维码全部保留。
- 原有用量工具、额度条与统计图表保留；不能为了简化首页删除其能力。

## 已修复的代码问题

1. 固定调度编号的展示错误地依赖 active；关闭参与后编号消失。现在编号与参与资格分开，关闭开关仍显示原编号，不改真实映射、不隐式加入调度。
2. 无映射和目录读失败都曾显示为空。现在分别为「未编号」「编号待核实」；冲突编号、重复 profile、重复归一别名及无效目录不能伪装成确认结果。
3. 卡片、列表、浮窗、详情采用同一编号展示规则；搜索支持字母编号，排序不会重新分配编号。
4. 系统镜像备注原来仅按同邮箱组取首个备注，可能跨工作区或随排序变化。现在必须同时匹配账号 ID 与规范化邮箱；存在冲突备注时回退到自身名称。
5. Windows 详情只有通用标题。现在透传所属目录的已有备注，重排和改备注后保持与同一个 profile 对应。
6. 日历移除时发现历史正文和翻译入口的可达性风险，已在近期记录中复用原有展开/翻译组件，并始终保留完整记录入口。

## 设计预览，尚未写入正式布局

采用「新骨架、原版视觉」：保留新的信息分区、首页列表/简洁卡片切换和完整管理页结构，沿用原图标、自定义头像、深灰配色、原生控件及额度/图表样式。避免新增大量彩色胶囊、圆形字母头像或无意义留白。

「推荐与公告」合并公开重置动态、AiGoodBro 公告和推荐 Skills，折叠只留紧凑一行；展开后分别查看，准确标注来源。第三方公开预告不冒称 OpenAI 官方确认。

首页保留会员到期日、5 小时和 7 天窗口剩余与各自重置时间、美元/点数及必要操作。完整模型与调度仍放管理页，默认展开。缺失值保持未知，不写成 0。

引导继续保留并优化为：添加 Codex 账号、登录授权、验证额度、决定是否参与调度，每步有明确入口与完成提示。

## 最新要求对照与出图约束

用户已指出：此前图片未充分综合后续意见，混用了早期方案。以下为后续整套设计的统一约束；先前图片仅供局部构图或视觉参考，均不能凭单项认可被视为整稿已确认。

| 项目 | 当前要求 | 下一套预览必须能核对的内容 |
| --- | --- | --- |
| 整体风格 | 新的信息结构，原有视觉与字体 | 沿用旧版系统字体、图标、自定义头像、深灰配色、原生按钮、额度条与用量图表；头像约 24px、CLI 图标约 18px，优先复用现有清晰 PNG；避免卡通化、成片彩色标签和过大的编号占位。 |
| 首页两种布局 | 简洁卡片与横向列表同时保留 | 同一套账号与字段在两种布局中一致，切换只改变排列；均紧凑，不用拉长进度条或撑高卡片填空。 |
| 首页必需信息 | 会员到期、5 小时和 7 天剩余、各自重置时间、美元及点数 | 层级清楚，日期与余额易于比较；不能以简化为由省略这些字段。倒计时与绝对时间来自同一重置时间；示例图明确标示示例数据。 |
| 账号识别与顺序 | 全部账号都有用户可自定义的显示编号 | 显示编号表达用户设置的排列顺序，三视图共用并持久保存；刷新、切布局、开关调度不自动改序。原内部账号 ID 与调度字母不被重写，也不再冒充这个显示编号；不同页面不能借用其他账号的名称。 |
| Codex 管理页 | 保留全部现有能力，重新整理排版 | 模型与调度默认展开；原额度统计工具、账号详情、浏览器、重置卡及现有其他功能保持可达。调度开关必须能关闭并正确持久化。 |
| 公共信息 | 合并为紧凑的「推荐与公告」区域 | 公开重置预告、AiGoodBro 公告、推荐 Skills 放在同一入口，折叠仅占一行，各分区独立收起；全局预告不能呈现为选中账号的个人重置记录。 |
| 删除边界 | 仅重置日历已获明确删除授权 | 所有界面不再出现日历；近期历史、原文、翻译、来源仍保留。其他删除须逐项讨论确认，不能用低频猜测代替授权。 |
| 保留与优化 | 截图、引导、全部致谢、关于 AiGoodBro | 首页可达截图；引导重点说明添加和验证 Codex 账号；关于页保留微信 AiGoodBro、复制按钮及二维码；不能裁掉致谢。 |
| 交互与准确性 | 可折叠、可关闭、信息可信 | 首页各分类能独立折叠；导航管理弹层有明确关闭入口；卡片底部按钮约 28px 高、图标约 14px，缩小视觉而不损害点击和键盘可达性；未知金额显示「—」，不混同真实 0；减少重复文案和浮点长尾。 |
| 多平台与协作 | 既有修复和 Windows 同步继续跟进 | Jev 与参与账号的后续审查都使用本清单及同一候选版本；图片不是功能验收，不代替 CLI 工作流检查、跨平台验证和安装发布。 |

出图交付规则：

1. 完整整套评审需要同版首页卡片、首页列表及 Codex 管理页；材质方向校准可先交付明确标注的首页代表页。不得把旧管理页混入新版或称为完整整套。
2. 先逐项核对本表，再展示；生成图里漏项、假值或错标不能被当作已落实的需求。
3. 用户认可某个骨架、图标或区域，只确认该部分，不能据此宣称完整设计已批准。
4. 用户已要求按完整要求重新出图。当前交付以本文件顶部版本为准；实际生成、看图核对与用户确认分别记录。0923v4 三视图属于历史稿。新布局尚未覆盖安装或发布。

### 0923v4 历史图稿规则

- 所有 8 个示例账号都显示序号 01–08，Pro 20x、Pro 5x 也有序号。提供「编号与排序」入口。此前图稿的「未编号」与字母徽标不再用于首页显示编号。
- 用户修改编号或排列后保存自己的顺序；额度刷新、卡片/列表切换、重启和调度开关不自动排序。内部 dispatchCode 与账号身份保持独立，用户显示序号不改变真实调度身份。
- 原字体优先；头像和操作图标降低视觉尺寸，按钮和其他 CLI 区域整体收紧。已确认项目存在 `Resources/codex-color.png` 等原 PNG 资源；图稿引用原图标，不以生成出的近似图替换正式资源。
- 本轮仅重绘设计预览，显示编号的数据模型、持久化、编辑冲突处理和重启验证尚未实施，不能把旧的调度字母修复宣称为新显示编号需求已经完成。
- D 账号已就最新编号、字体和尺寸要求再次完成独立文字审查，正文及成功回执哈希已核对、租约已收尾。采纳唯一性校验、拖动和编号编辑共用一份顺序、PNG 高分辨率资源与焦点状态要求。其「删除后自动连续重编号」建议暂不采纳：为保护用户自定义结果，保留其余编号与顺序，新账号追加，冲突明确处理；需要用户改变编号时再执行。
- 0923v4 三张当前候选图已生成并逐图看过：`home-cards-0923v4.png`、`home-list-0923v4.png`、`codex-management-0923v4.png`。均已保存到本轮项目产物目录，PNG 尺寸与复制哈希已核对。当时交付入口为该目录的 `PREVIEW_INDEX.md`；目前已被本文件顶部版本替代，旧三图不作为当前交付。
- 图面检查覆盖三视图显示序号 01–08、Pro 5x 标签、未知额度不画零进度、到期/重置信息、美元/点数标签、公告合并和原功能入口。已修正品牌图标误用、状态颜色及模型字段重复。生成图不证明原生字体/控件、图标文件引用、持久化或动态操作已经实现，审美仍待用户评阅。

### 0923v3 本轮协作与统一数据

- D 账号已通过本轮新鲜参与、身份、额度、占用门禁，并实际用隔离只读 CLI 完成最小返回验证及独立需求审查。请求参数为 gpt-5.6-sol / high / default，未切换桌面账号；结果退出 0，回执正文哈希匹配，两个租约均已验收收尾。审查仅涉及文字方案，不能称其看过图。
- 新增审查特别要求：Pro 5x 不得误标 Plus；未知窗口不画零进度；折叠分区彼此独立；导航弹层关闭不等于退出应用；保留功能必须能找到入口。
- Jev 文字判断约 1.27 秒，选择首页紧凑、管理页模型调度默认展开（置信度 0.82）；这只是对需求顺序的解释建议，不是视觉判断或用户批准。
- 三图共用 8 个示例账号、示例时刻 2026-09-23 03:00 北京时间；重置日期及倒计时在生成前按同一时间计算。Pro 20x 与 Pro 5x 的缺失 5 小时窗口均明确未提供，未知金额保留「—」，已确认示例零值才写 0。
- 本轮协作、共享示例数据、提示词与图稿验收记录位于项目既有 `.local-artifacts/ui-layout-preview-0922v1/consolidated-0923v3/`。这是设计预览，不是线上账号快照。

## Jev 与账号协作

实际调用 Jev 文字评审，请求模型 jev-1.13.0，20 秒上限、无自动重试；本轮特征取舍调用耗时约 1.2 秒。编号建议选择固定映射展示（置信度 0.99）；本地重置计数控制建议收纳（0.88）；优先标记结论为证据不足，不能据此删除；专属浏览器建议保留（0.61）；推荐区收纳建议置信度较低（0.44）。这些是建议，不是使用频率或 GUI 验证结果。

实际 D 账号独立完成两轮文字审查，任务参数 gpt-5.6-sol / high / default，隔离只读入口；成功回执、正文哈希和产物已核对并验收收尾。未声称运行事件报告了实际模型版本，也未声称该账号看过效果图。主代理根据代码与验证裁定，不以投票替代证据。

独立代码复核补出重复 alias 和历史正文/翻译入口问题，修复后复核。没有实际功能使用频率统计，所以当前建议只删除已确认日历，其余先合并或折叠；低频控件的进一步调整留待用户确认。

## 验证与边界

- Mac 最终候选构建成功，32/32 纯自检通过；覆盖固定编号、冲突映射、镜像名称与排序、重置历史/北京时间边界，并保留原有截图、引导、图表与 WebView 检查。
- Windows Web 构建通过；31 项单元测试、4 项相关渲染回归通过，覆盖日历消失、保留公告全文/倒计时/来源，以及重排/改备注后的详情归属。
- 已渲染 Mac 原生候选在 820/980/1280 宽度、明暗主题的预览，检查窄窗账号标题与保留编号状态。预览不是实账号调度或登录验证。
- Windows 原生 WebView2 与安装包仍需 Windows 环境验证；没有把浏览器测试写成原生安装已验收。
- 最终纯自检结果在本任务本地产物记录；不声称已经消除所有未知缺陷。
