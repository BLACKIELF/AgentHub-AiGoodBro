# 0916v1 · 设备授权与账号按钮尺寸 · 9.6.1 (50)

相对 0915v5：Codex 重新登录与新增独立账号共用设备授权码引导。复制授权码、重开 Chrome、到期重新生成、取消与登录后验证均显示当前阶段；登录目标固定，切换监控账号不会改变授权对象。原身份匹配、维护占用、凭据保护和失败回滚保留，只有读取到新额度才显示完成。

账号行的刷新、暖号、终端、监控和桌面五个按钮共用等宽单元格与 24 pt 高度；刷新或暖号加载时保持同样尺寸。

验证：设备码解析、真实本地 CLI 的合成管道输出、进程取消/超时/清理、身份与凭据保护、过期重试及相关自测通过；macOS arm64 优化构建、签名和资源校验通过。40 张中英文、深浅色合成截图覆盖引导状态与账号布局，原生合成窗口完成交互验收。

真实账号的 Chrome 授权、系统剪贴板复制、登录身份与新额度仍需各自实机验收。此记录不代表已经发布 Release 或完成真实登录；内部版本号沿用 9.6.1 (50)，V1.0 标签不移动。

## 0915v5 · 9.6.1 (50)

相对 0915v4：OpenCode 已保存 API 或 OAuth 服务商配置时直接复用，重新检测不再打开登录；引导入口复用当前配置，账号菜单的「添加或更新服务商」保留主动更新入口。额度未知不影响打开终端。Codex 账号切换、刷新及认证实现未改动。

回归覆盖已有配置不启动认证、明确更新仍能进入官方入口、原凭据保持。修正 CI 中仍把 ZCode 当成 CLI 的旧断言，验证桌面入口可用且没有 CLI 登录能力；不改变 ZCode 产品行为。

`V1.0` 为用户指定的源码里程碑标签；本次应用构建仍使用上述内部版本号。标签在本次修复、安装和 CI 完成后标记，不代表另行发布安装包。

## 0915v4 · 9.6.1 (49)

相对 0915v3，修复已完成 Gemini 登录仍显示等待、非 Codex CLI 按钮被额度状态限制，以及安装包缺少配套组件的问题。Codex 账号、切换和刷新实现保持原样。

- Gemini Google 登录与 API Key 配置分别识别；API Key 不再因缺少 Code Assist 订阅额度而显示登录待确认。OpenCode 按已配置服务商识别，不以 OpenCode Go 额度推断其他服务商登录。
- 登录期间每两秒检查本地配置，返回应用也会重查；发现凭据即可继续清单，保留交互终端。Claude、Kimi、Grok 读取各自现有配置，ZCode/TRAE 继续由用户确认桌面登录。
- 已安装且支持的官方工具可直接打开，额度读取失败或正在刷新不阻塞终端入口。保存凭据、额度可用与模型实际成功仍分别显示和验收。
- 补齐随包连接说明并默认打包本机调度组件。打包及 CI 校验安装器所需文件，测试从实际打包清单构造资源，防止源码有文件而安装包漏文件。
- 修正图表高度回调的主线程调度，兼容 CI 使用的较旧 macOS SDK。

针对新增登录检测和最终随包安装验证；不自动发模型请求。模型调用与原生登录操作由用户验收，未接通的工具不会标为已验证。

---

# 0915v3 · 9.6.1 (48)

相对 0915v2：Token 按页签内容调整高度，概览收紧，明细新增工具、模型和 Token 分项图，原始表格按需展开。重置日历收紧无公告提示。所有账号卡片共用紧凑/标准/宽松大小设置；隐藏系统登录镜像卡，不删除账号资料。终端按钮仅保留图标和下箭头，菜单保持。

首次弹窗和“使用引导”共用工具连接清单，可多选并逐项完成官方登录。已读取账号、用户自行确认和额度分别显示；交互式终端完成授权后可返回继续，不必等待整个 TUI 退出。ZCode 打开经产品标识核对的桌面应用，停止调用包内 CLI；OpenCode 保留官方 CLI 登录。配套 Skill 新增对应连接流程，受管安装继续保护原有用户文件。

验证范围：改动资源与生产入口的离线回归、构建、签名和覆盖安装；原生引导与 CLI 登录由用户操作验收。原手动/自动桌面切换与刷新实现未改动，以差异比较确认保留。

---

# AiGoodBro 9.6.1 (47) · 0915v2

相对 9.6.0 (45) / 0913v10，首页用量展示集中于 Token，公告进入重置日历，右栏接入 AI 热点；补充其他 CLI 的官方登录入口和 Claude Code 图标的主题适配。

- 手动切换桌面立即开始，不再等待额度刷新；取消本轮读取并拒绝旧结果回写。当前桌面账号卡也可重新核对切换，占用或登录冲突会直接说明原因。
- 设置中可选默认、1/3/5/10/30 分钟额度刷新，改变设置立即重排刷新时间并保存。
- 「额度不足自动换号」需开启：按桌面账号判断而非正在监控的账号；核对备用额度，两个窗口均可用且触发窗口至少剩余 30%。Codex 离开前台至少两分钟、无活跃任务且能确认对话恢复时，共用手动切换的原子写入、核验、回滚和恢复流程。关闭开关或状态改变会阻止尚未开始的切换。

- Token 总量右侧显示今日、近 7 天、本月的已记录用量与有记录天数，沿用统计引擎汇总，不重复累计各来源。未采集与已记录为零分别显示。
- 年度热力图悬停显示日期及当天总量，点击固定右侧工具明细。维度总和与当天总量一致时显示占比；覆盖不完整时显示已记录数值。Token 面板移除重置公告。
- 趋势保留原有竖向堆叠柱状图、工具 / 模型分组与起止日期。更新图表时继续保留选择和页签。
- 重置日历下方显示所选日公告并提供完整历史，翻月与返回今天同步。右栏显示 AIHOT 热点标题、来源数量与时间，至少间隔 5 分钟刷新，支持条件请求与失败保留上次结果。
- Claude Code 使用官方 `auth login`；Gemini 打开交互认证入口；Kimi 对新旧 CLI 设置对应配置目录变量。Claude / Gemini 的关联目录保持只读，不将目录关联误示为完整账号隔离。
- WorkBuddy 国内版与国际版分别发现，使用各自应用、产品配置及数据目录；不静默替换另一产品。额度未接通时不再直接宣称已登录。
- Claude Code 图标复用已有模板资源，随深浅色模式显示前景和底板。

AIHOT 按 [官方接口文档](https://aihot.news/agent) 接入匿名只读热点接口，仅展示摘要元数据并链接来源。当前用途为个人非商业；对外商业或公开镜像等用途另依官方条件处理。

本文件记录源码候选，不代表已覆盖安装、真实 CLI 登录、Windows 原生验收或 GitHub 发布。原切号、长截图和整行折叠修复继续保留，原生操作由用户执行。
