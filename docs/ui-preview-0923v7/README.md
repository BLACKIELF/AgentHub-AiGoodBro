# AiGoodBro 原有主题与毛玻璃提案 0923v8

相较 0923v7：按用户最新要求，配色恢复为 AiGoodBro 原有的 7 套主题，不再使用回音谷参考中的四套新色盘。保留紧凑卡片与列表骨架，增加磨砂玻璃表面、半透明导航、柔和边缘高光和浅阴影。继续使用系统字体与既有图标，不改变账号信息层级。

为保持已打开的预览链接有效，仍使用本目录的 `index.html`，版本以页面标题与页脚 0923v8 为准。按用户确定的默认值，首次打开使用深色「液态键帽」。右上角可切换原有主题和深浅外观，账号区域可切换卡片／列表。

原有主题：默认、液态键帽、青花瓷、蒙特雷曙霞、千里江山、敦煌飞天、故宫红墙。主色、辅色、选中态与两组额度渐变直接来自项目 `Resources/Palettes` 的深浅配色文件。页面玻璃表面为本次设计提案；背景透光与描边负责层次，文字保持实色，低额度使用独立警示色。减少透明度／增强对比度时使用实色表面，缺少毛玻璃支持时也有实色回退。

保留固定编号、会员到期日、美元／点数、5 小时与 7 天剩余额度及重置时间；保留紧凑推荐与公告、其他 CLI、添加账号指南、关于和致谢入口。此稿不增加或删除应用功能。

账号、额度、日期与倒计时都是示例数据。主题、深浅外观和卡片／列表切换可操作，其余按钮只展示视觉位置。本 HTML 是设计稿，不能作为真实应用运行验证或安装完成的证明。

后续原生实现、独立安装和验证范围见 [原生界面实现 0923v8](../ui-implementation-0923v8.md)。该实现复用真实 SwiftUI 视图；独立体验版仍使用合成账号，不代表真实账号操作已通过验收。此前已发送的单文件 HTML 保留原样，本目录 `index.html` 是当前对照入口。
