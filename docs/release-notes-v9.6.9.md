# 9.6.9 / 0922v4

相对 9.6.8（57）：新增秒级真实倒计时，并将 9.6.6–9.6.9 源码与 Windows 对齐改动集中到同一交付分支。

- Mac 公开预告、账号 5 小时/7 天窗口从绝对截止时间计算剩余天、时、分、秒，睡眠或漏帧后重新按当前时间计算。
- 公开预告到点显示“等待来源确认”；个人窗口到点显示“等待额度更新”。不因为计时结束写入重置历史、增加额度或兑换重置卡。
- 新计时器独立刷新小范围文字，不触发每秒账号/网络查询；未知日期保留未知。
- 保留前版主页布局、独立折叠、默认展开模型与调度、调度关闭修复、账号信息、登录指引与 About 微信复制/完整二维码。
- Windows 增加同口径秒级计时、公开预告/历史/维护者消息、独立首页折叠、推荐入口、账号官方套餐和余额详情、可关闭的添加账号指引，以及同一 About 联系方式。Windows 真实可用范围及完整执行指令见 [Windows AI 一次执行交接](windows-port/WINDOWS_AI_HANDOFF_0922v4.md)。

## 验证

Mac 原生 32 项自检通过，9.6.9（58）已在本机保留旧版备份后覆盖安装，代码签名和可执行文件摘要核验一致。实际窗口观察 115 秒，倒计时减少 114 秒，误差在一个界面刷新周期以内，截图未见文字叠印。

Windows 前端构建、单元/契约测试和合成数据浏览器交互持续复验；原生 Rust、PowerShell、WebView2 和安装包结果分别记录，不能用浏览器通过代替真机通过。

Jev 1.13.0 参与窄问题与界面动作选择；指定 Pro 5x 账号以 GPT-5.6 Sol / High 完成余额、套餐与计时解析复核。前两次协作运行未取得完整交付，不计为通过；之后的窄范围审查有真实完成回执。审查意见以实际契约、代码和测试逐条判断。
